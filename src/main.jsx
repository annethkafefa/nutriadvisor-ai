import React from "react";
import ReactDOM from "react-dom/client";
import NutriAdvisor from "./NutriAdvisor";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <NutriAdvisor />
  </React.StrictMode>
);
